export const requiredString = {
  type: String,
  required: true,
  trim: true,
};

export const requiredNumber = {
  type: Number,
  required: true,
};

export const nonRequiredNumber = {
  type: Number,
};

export const nonRequiredString = {
  type: String,
  trim: true,
};

